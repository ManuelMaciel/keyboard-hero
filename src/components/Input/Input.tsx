import styled from "styled-components";

const Input = () => {
  return (
    <div>
      <p>Hola Mundo</p>
    </div>
  )
}

// Styled Components
const Input = styled.input`

`

export default Input;
